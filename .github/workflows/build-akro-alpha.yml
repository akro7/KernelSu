# ═══════════════════════════════════════════════════════════════
#  AKRO Alpha – GitHub Actions Build Workflow
# ═══════════════════════════════════════════════════════════════
name: Build AKRO Alpha

on:
  push:
    branches: [ "main", "master", "dev" ]
    paths:
      - 'manager/**'
      - '.github/workflows/build-akro-alpha.yml'
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:
    inputs:
      release:
        description: 'Create GitHub Release'
        required: false
        default: 'false'
        type: boolean

jobs:
  build-manager:
    name: 🔨 Build AKRO Alpha APK
    runs-on: ubuntu-latest

    # ── Expose secrets as env so they can be checked in steps ──
    env:
      KEYSTORE_FILE:     ${{ secrets.KEYSTORE_FILE }}
      KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
      KEY_ALIAS:         ${{ secrets.KEY_ALIAS }}
      KEY_PASSWORD:      ${{ secrets.KEY_PASSWORD }}

    steps:
      # ── 1. Checkout ─────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: 'recursive'

      # ── 2. Java 21 ──────────────────────────────────────────
      - name: Setup JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      # ── 3. Android SDK ──────────────────────────────────────
      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      # ── 4. Android NDK ──────────────────────────────────────
      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r27c
          add-to-path: true

      # ── 5. Rust ─────────────────────────────────────────────
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      # ── 6. Gradle Cache ─────────────────────────────────────
      - name: Cache Gradle
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle.kts', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      # ── 7. Get version info ─────────────────────────────────
      - name: Get version
        id: version
        run: |
          VERSION_NAME=$(grep -oP '(?<=managerVersionName = ")[^"]+' manager/build.gradle.kts 2>/dev/null || echo "dev")
          VERSION_CODE=$(grep -oP '(?<=managerVersionCode = )\d+' manager/build.gradle.kts 2>/dev/null || echo "1")
          SHORT_SHA=$(git rev-parse --short HEAD)
          echo "name=${VERSION_NAME}" >> $GITHUB_OUTPUT
          echo "code=${VERSION_CODE}" >> $GITHUB_OUTPUT
          echo "sha=${SHORT_SHA}" >> $GITHUB_OUTPUT
          echo "📦 AKRO Alpha v${VERSION_NAME} (${VERSION_CODE}) @ ${SHORT_SHA}"

      # ── 8. Build Debug APK ──────────────────────────────────
      - name: Build Debug APK
        working-directory: manager
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          chmod +x gradlew
          ./gradlew :app:assembleDebug --no-daemon --stacktrace

      # ── 9. Check if keystore secret exists ──────────────────
      - name: Check keystore availability
        id: keystore_check
        run: |
          if [ -n "$KEYSTORE_FILE" ] && [ "${{ github.event_name }}" != "pull_request" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi

      # ── 10. Build Release APK ───────────────────────────────
      - name: Build Release APK
        if: steps.keystore_check.outputs.available == 'true'
        working-directory: manager
        env:
          ANDROID_NDK_HOME: ${{ steps.setup-ndk.outputs.ndk-path }}
        run: |
          echo "$KEYSTORE_FILE" | base64 -d > /tmp/release.jks
          ./gradlew :app:assembleRelease \
            --no-daemon \
            -Pkeystore.file=/tmp/release.jks \
            -Pkeystore.password="$KEYSTORE_PASSWORD" \
            -Pkey.alias="$KEY_ALIAS" \
            -Pkey.password="$KEY_PASSWORD"
          rm -f /tmp/release.jks

      # ── 11. Collect & rename outputs ────────────────────────
      - name: Collect APK files
        run: |
          mkdir -p artifacts
          find manager/app/build/outputs/apk -name "*.apk" | while read f; do
            base=$(basename "$f" .apk)
            cp "$f" "artifacts/AKRO_Alpha_${{ steps.version.outputs.name }}_${{ steps.version.outputs.sha }}_${base}.apk"
          done
          ls -lh artifacts/

      # ── 12. Upload artifacts ─────────────────────────────────
      - name: Upload APK artifacts
        uses: actions/upload-artifact@v7
        with:
          name: AKRO-Alpha-${{ steps.version.outputs.name }}-${{ steps.version.outputs.sha }}
          path: artifacts/*.apk
          retention-days: 30

      # ── 13. GitHub Release (manual trigger only) ─────────────
      - name: Create GitHub Release
        if: github.event.inputs.release == 'true'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ steps.version.outputs.name }}-${{ steps.version.outputs.sha }}
          name: "AKRO Alpha v${{ steps.version.outputs.name }}"
          body: |
            ## AKRO Alpha – Automatic Build

            **Version:** `${{ steps.version.outputs.name }}` (`${{ steps.version.outputs.code }}`)
            **Commit:** `${{ steps.version.outputs.sha }}`
            **Branch:** `${{ github.ref_name }}`

            ### Changes
            ${{ github.event.head_commit.message }}

            ---
            > Built by GitHub Actions 🤖
          files: artifacts/*.apk
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
